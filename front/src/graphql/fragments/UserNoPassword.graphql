fragment UserNoPassword on UserNoPassword {
  currentQuestion {
    ...QuestionInfo
  }
  answeredQuestions {
    ...QuestionInfo
  }
  email
  score
  createdAt
  lastLogin
  role
  username
  fullname
  _id
  level
  role
}
