mutation login($option: UserLoginInfos!) {
  login(options: $option) {
    user {
      _id
      username
      email
      createdAt
      lastLogin
      level
      role
    }
    errors {
      field
      message
    }
    token
  }
}
