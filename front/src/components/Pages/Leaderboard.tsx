import React from 'react'

const Leaderboard= () => {
    return (
        <div>
            THis is the leaderboards
            # We Fetch all users  (or refetch)
            # We Display them sorted based on their score while giving also their level
        </div>
    )
}

export default Leaderboard
