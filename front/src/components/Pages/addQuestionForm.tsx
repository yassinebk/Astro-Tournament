import React from 'react'

const addQuestionForm= () => {
    return (
        <div>
           Hello this is the add Question Form it should be wrapped by either a modal or a form 
        </div>
    )
}

export default addQuestionForm
