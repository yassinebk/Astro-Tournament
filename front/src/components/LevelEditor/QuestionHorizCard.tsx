import { Box } from "@chakra-ui/layout";
import React from "react";
import { Questions } from "../../generated/graphql";

interface QuestionHorizCardProps {
  question: Questions;
}

const QuestionHorizCard: React.FC<QuestionHorizCardProps> = ({}) => {
  return <Box></Box>;
};

export default QuestionHorizCard;
