import { makeVar } from "@apollo/client";


export const setNotification = makeVar(null);

export const user = makeVar(null);
export const token = makeVar(null);


export const questions = makeVar([]);
export const levels = makeVar([])